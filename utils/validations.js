export const allowed_photo_extensions = ['jpg', 'png', 'jpeg']

export const allowed_photo_max_size = 2097152;

export const capitalizedFirstLetter = (word) => (word.charAt(0).toUpperCase() + word.slice(1))